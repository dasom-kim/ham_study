org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Opt out of AGP 9+ to fix compatibility with old plugins
android.useNewApkCreator=true
android.useOldApkApproach=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Suppress warnings for old plugins using deprecated repositories
android.suppressUnsupportedCompileSdkWarning=true
# Initialize gradle with custom init script
org.gradle.initialization.include-build=gradle
